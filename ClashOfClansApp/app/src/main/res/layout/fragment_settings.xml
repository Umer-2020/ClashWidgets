<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_dark"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Settings"
            android:textColor="@color/gold_primary"
            android:textSize="20sp"
            android:textStyle="bold"
            android:layout_marginBottom="16dp" />

        <!--
            Phase 3 will add:
              • Builder's Apprentice level picker (1–8)
              • Lab Assistant level picker (1–12)
              • Goblin Event toggle switch
              • Total Builders count selector (5 or 6)
        -->
        <TextView
            android:id="@+id/placeholder_settings"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="[ Helper level selectors + Goblin Event toggle – Phase 3 ]"
            android:textColor="@color/text_hint"
            android:padding="12dp"
            android:background="@color/card_bg" />

    </LinearLayout>
</ScrollView>
